export class Alien {
  id: string;
  caption: string;
  type: string;

  constructor(id: string, caption: string, type: string) {
    this.id = id;
    this.caption = caption;
    this.type = type;
  }
}
